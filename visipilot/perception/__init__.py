"""Perception stage: UI element detection + OCR, operating strictly on
screenshot pixels. Never imports browser/DOM access — see the pixels-first
rule in Instructions.md #2 and the "Perception" module boundary in
README.md.
"""
