"""Target Selection: parses the user instruction and ranks candidate
UIElements from a SemanticUIState. Pure reasoning over the structured
representation — never over raw pixels or the DOM. See the "Target
Selection" module boundary in README.md.
"""
