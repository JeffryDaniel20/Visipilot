"""Trace Recorder: orchestrates one full instruction run across every
pipeline stage built so far, with structured logging and a TraceRecord
written to disk for every run. See the "Trace Recorder" module boundary
in README.md.
"""
